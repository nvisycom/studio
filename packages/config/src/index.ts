// @nvisy/config — shared configuration and constants.
//
// Add exports here as shared config is introduced. This package is built with
// tsdown (ESM, platform-neutral) and is not yet consumed by the apps.

export {};

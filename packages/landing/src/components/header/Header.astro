---
import Logo from "../Logo.astro";
import DesktopNavigation from "./DesktopNavigation.vue";
import MobileNavigation from "./MobileNavigation.vue";
---

<header
  class="sticky top-0 z-50 w-full border-b border-gray-200/50 dark:border-neutral-700/50 bg-white/95 dark:bg-black/95 backdrop-blur-md supports-[backdrop-filter]:bg-white/90 dark:supports-[backdrop-filter]:bg-black/90 shadow-sm"
>
  <div class="container mx-auto px-4 md:px-8 lg:px-12">
    <div class="flex h-16 items-center justify-between gap-8">
      <!-- Logo Section -->
      <Logo asLink={true} size="md" />

      <!-- Desktop Navigation Section -->
      <DesktopNavigation client:load />

      <!-- Auth Button Section -->
      <div class="flex items-center space-x-3">
        <a
          href="/signin"
          class="hidden sm:inline-flex relative text-gray-600 dark:text-neutral-300 hover:text-gray-900 dark:hover:text-white font-medium text-base transition-all duration-300"
        >
          Sign In
        </a>
        <a
          href="/signup"
          class="inline-flex items-center justify-center bg-black dark:bg-white hover:bg-gray-800 dark:hover:bg-gray-200 text-white dark:text-black font-medium px-4 py-2 text-base transition-all duration-300 rounded-md no-underline hover:shadow-lg hover:scale-105"
        >
          Sign Up
        </a>

        <!-- Mobile Navigation Component -->
        <MobileNavigation client:load />
      </div>
    </div>
  </div>

  <!-- Mobile Navigation Menu (Full Width) -->
  <MobileNavigation menuOnly={true} client:load />
</header>

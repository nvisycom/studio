---
import { ArrowRight } from "lucide-vue-next";

export interface PricingTier {
	name: string;
	price: string;
	description: string;
	features?: string[];
}

export interface PricingCTA {
	text: string;
	href: string;
	variant?: "primary" | "secondary";
}

export interface Props {
	title: string;
	description: string;
	tiers: PricingTier[];
	ctas: PricingCTA[];
	backgroundColor?: "white" | "gray";
	compact?: boolean;
}

const {
	title,
	description,
	tiers,
	ctas,
	backgroundColor = "white",
	compact = false,
} = Astro.props;

const bgClass =
	backgroundColor === "gray"
		? "bg-neutral-50 dark:bg-neutral-950"
		: "bg-white dark:bg-black";
---

<div class={`${bgClass} py-20`}>
  <div class="container mx-auto px-4">
    <div class="max-w-4xl mx-auto text-center">
      <h2 class="text-3xl md:text-4xl font-bold text-neutral-900 dark:text-white mb-6">
        {title}
      </h2>
      <p class="text-lg text-neutral-600 dark:text-neutral-300 mb-12">
        {description}
      </p>

      <div class={`rounded-2xl p-8 border border-neutral-200 dark:border-neutral-700 mb-12 ${
        backgroundColor === 'gray'
          ? 'bg-white dark:bg-black'
          : 'bg-neutral-50 dark:bg-neutral-900'
      }`}>
        <div class={`grid gap-8 ${
          tiers.length === 3
            ? 'grid-cols-1 md:grid-cols-3'
            : 'grid-cols-1 md:grid-cols-2'
        }`}>
          {tiers.map((tier) => (
            <div class="text-center">
              <div class="text-3xl font-bold text-neutral-900 dark:text-white mb-2">
                {tier.price}
              </div>
              <div class="text-neutral-600 dark:text-neutral-300 mb-2">
                {tier.name}
              </div>
              {!compact && (
                <div class="text-sm text-neutral-500 dark:text-neutral-400">
                  {tier.description}
                </div>
              )}
              {tier.features && !compact && (
                <div class="mt-4">
                  <ul class="space-y-2 text-sm text-neutral-600 dark:text-neutral-300">
                    {tier.features.map((feature) => (
                      <li>• {feature}</li>
                    ))}
                  </ul>
                </div>
              )}
            </div>
          ))}
        </div>
      </div>

      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        {ctas.map((cta) => (
          <a
            href={cta.href}
            class={`inline-flex items-center justify-center font-medium py-3 px-8 rounded-lg transition-colors ${
              cta.variant === 'primary'
                ? 'bg-black dark:bg-white text-white dark:text-black hover:bg-neutral-800 dark:hover:bg-neutral-100'
                : 'border-2 border-neutral-300 dark:border-neutral-600 text-neutral-700 dark:text-neutral-300 hover:bg-neutral-50 dark:hover:bg-neutral-900'
            }`}
          >
            {cta.text}
            <ArrowRight class="w-5 h-5 ml-2" />
          </a>
        ))}
      </div>
    </div>
  </div>
</div>

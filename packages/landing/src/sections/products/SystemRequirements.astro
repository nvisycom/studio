---
export interface RequirementSpec {
	label: string;
	value: string;
}

export interface RequirementSet {
	title: string;
	description?: string;
	specs: RequirementSpec[];
	highlighted?: boolean;
}

export interface Props {
	title: string;
	description: string;
	requirementSets: RequirementSet[];
}

const { title, description, requirementSets } = Astro.props;
---

<div class="bg-white dark:bg-black py-20">
  <div class="container mx-auto px-4">
    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 dark:text-white mb-6">
          {title}
        </h2>
        <p class="text-lg text-gray-600 dark:text-gray-300">
          {description}
        </p>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
        {requirementSets.map((requirementSet) => (
          <div class={`rounded-2xl p-8 ${
            requirementSet.highlighted
              ? 'bg-gray-100 dark:bg-gray-800 border border-gray-200 dark:border-gray-700'
              : 'bg-gray-50 dark:bg-gray-900'
          }`}>
            <h3 class="text-2xl font-bold text-gray-900 dark:text-white mb-6">
              {requirementSet.title}
            </h3>
            {requirementSet.description && (
              <p class="text-gray-600 dark:text-gray-300 mb-6">
                {requirementSet.description}
              </p>
            )}
            <div class="space-y-4">
              {requirementSet.specs.map((spec) => (
                <div class={`flex justify-between items-center py-3 border-b ${
                  requirementSet.highlighted
                    ? 'border-gray-200 dark:border-gray-600'
                    : 'border-gray-200 dark:border-gray-700'
                }`}>
                  <span class="text-gray-700 dark:text-gray-300">{spec.label}:</span>
                  <span class="font-medium text-gray-900 dark:text-white">{spec.value}</span>
                </div>
              ))}
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
</div>

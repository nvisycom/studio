---
import { ArrowRightIcon } from "lucide-vue-next";

export interface Props {
	title: string;
	description: string;
	primaryCta: {
		text: string;
		href: string;
	};
	secondaryCta: {
		text: string;
		href: string;
	};
	stats: Array<{
		value: string;
		label: string;
	}>;
}

const { title, description, primaryCta, secondaryCta, stats } = Astro.props;
---

<div class="relative bg-white dark:bg-black">
  <div class="container mx-auto px-4 py-20 md:py-32">
    <div class="max-w-5xl mx-auto text-center">
      <h1 class="text-5xl md:text-7xl font-bold text-neutral-900 dark:text-white mb-8 tracking-tight">
        {title}
      </h1>

      <p class="text-xl md:text-2xl text-neutral-600 dark:text-neutral-300 leading-relaxed mb-12 max-w-4xl mx-auto">
        {description}
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center mb-20">
        <a
          href={primaryCta.href}
          class="inline-flex items-center justify-center bg-black dark:bg-white text-white dark:text-black font-medium py-4 px-8 rounded-lg hover:bg-neutral-800 dark:hover:bg-neutral-100 transition-colors text-lg"
        >
          {primaryCta.text}
          <ArrowRightIcon class="w-5 h-5 ml-2" />
        </a>
        <a
          href={secondaryCta.href}
          class="inline-flex items-center justify-center border-2 border-neutral-300 dark:border-neutral-600 text-neutral-700 dark:text-neutral-300 font-medium py-4 px-8 rounded-lg hover:bg-neutral-50 dark:hover:bg-neutral-900 transition-colors text-lg"
        >
          {secondaryCta.text}
        </a>
      </div>

      <!-- Key Stats -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-3xl mx-auto">
        {stats.map((stat) => (
          <div class="text-center p-6 border border-neutral-200 dark:border-neutral-700 rounded-lg">
            <div class="text-3xl md:text-4xl font-bold text-neutral-900 dark:text-white mb-2">{stat.value}</div>
            <div class="text-neutral-600 dark:text-neutral-400">{stat.label}</div>
          </div>
        ))}
      </div>
    </div>
  </div>
</div>

import type { RasterPolicy } from "@nvisy/sdk/datatypes";

/** Raster policy options, in display order (Always / Automatic / Never). */
export const RASTER_POLICIES: RasterPolicy[] = ["always", "auto", "never"];

---
interface StatItem {
	value: string;
	label: string;
	description?: string;
}

export interface Props {
	title?: string;
	stats?: StatItem[];
	inline?: boolean;
	padding?: "sm" | "md" | "lg" | "xl";
	maxWidth?: "sm" | "md" | "lg" | "xl" | "full";
}

const {
	title,
	stats,
	inline = false,
	padding = "xl",
	maxWidth = "xl",
} = Astro.props;

const defaultStats: StatItem[] = [
	{ label: "Founded", value: "2025" },
	{ label: "Enterprise Customers", value: "10+" },
	{ label: "Documents Processed", value: "1M+" },
	{ label: "Team Members", value: "3" },
];

const statsData = stats || defaultStats;

const bgClass = inline ? "" : "bg-white dark:bg-black";

const paddingClass = inline
	? ""
	: padding === "sm"
		? "py-12 md:py-16"
		: padding === "md"
			? "py-16 md:py-24"
			: padding === "lg"
				? "py-20 md:py-28"
				: "py-24 md:py-32";

const maxWidthClass =
	maxWidth === "sm"
		? "max-w-2xl"
		: maxWidth === "md"
			? "max-w-4xl"
			: maxWidth === "lg"
				? "max-w-5xl"
				: maxWidth === "xl"
					? "max-w-6xl"
					: "max-w-none";

const gridCols =
	statsData.length <= 2
		? "grid-cols-1 sm:grid-cols-2"
		: statsData.length === 3
			? "grid-cols-1 sm:grid-cols-3"
			: "grid-cols-1 sm:grid-cols-2 lg:grid-cols-4";
---

{inline ? (
  <div class={`${maxWidthClass} mx-auto`}>
    {title && (
      <div class="text-center mb-8">
        <h2 class="text-3xl md:text-4xl lg:text-5xl font-light text-neutral-900 dark:text-white">
          {title}
        </h2>
      </div>
    )}
    <div class={`grid ${gridCols} gap-6 md:gap-8`}>
      {statsData.map((stat) => (
        <div class="text-center p-4 md:p-6 rounded-lg border border-neutral-200 dark:border-neutral-700">
          <div class="text-2xl md:text-3xl lg:text-4xl font-bold mb-2 text-neutral-900 dark:text-white">
            {stat.value}
          </div>
          <div class="text-sm md:text-base text-neutral-700 dark:text-neutral-300 font-medium">
            {stat.label}
          </div>
          {stat.description && (
            <div class="text-xs md:text-sm text-neutral-500 dark:text-neutral-500 mt-1">
              {stat.description}
            </div>
          )}
        </div>
      ))}
    </div>
  </div>
) : (
  <section class={`${paddingClass} ${bgClass}`}>
    <div class="container mx-auto px-4">
      <div class={`${maxWidthClass} mx-auto`}>
        {title && (
          <div class="text-center mb-12">
            <h2 class="text-3xl md:text-4xl lg:text-5xl font-light text-neutral-900 dark:text-white mb-4">
              {title}
            </h2>
          </div>
        )}
        <div class={`grid ${gridCols} gap-6 md:gap-8`}>
          {statsData.map((stat) => (
            <div class="text-center px-4">
              <div class="text-2xl sm:text-3xl md:text-4xl font-bold text-neutral-900 dark:text-white mb-2">
                {stat.value}
              </div>
              <div class="text-sm sm:text-base text-neutral-700 dark:text-neutral-300 font-medium">
                {stat.label}
              </div>
              {stat.description && (
                <div class="text-xs sm:text-sm text-neutral-500 dark:text-neutral-500 mt-1">
                  {stat.description}
                </div>
              )}
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>
)}

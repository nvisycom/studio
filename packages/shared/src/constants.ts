// Shared constants for the nvisy.com monorepo

/** Default words per minute for reading time calculation */
export const WORDS_PER_MINUTE = 238;

/** Maximum length for initials */
export const MAX_INITIALS_LENGTH = 2;
